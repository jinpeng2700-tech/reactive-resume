# Customizations

No application customizations are active in this clean baseline.

Private application changes must be documented here before release. Deployment configuration belongs under `deploy/` and is reviewed separately from upstream source merges.
